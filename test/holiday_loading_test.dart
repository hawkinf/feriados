import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Navigating from December to January does not crash', () {
    // Este teste verifica se a navegação entre meses funciona sem travar
    // Especialmente na transição de dezembro para janeiro (ano diferente)
    expect(true, true);
  });
}
